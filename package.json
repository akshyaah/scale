{
  "name": "venturepilot-ai",
  "version": "1.0.0",
  "description": "VenturePilot AI - Your AI Co-Founder for Smarter Financial and Business Decisions",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm run start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "build-frontend": "npm install --prefix frontend && npm run build --prefix frontend",
    "prepare-pages": "node scripts/prepare-pages.js",
    "deploy-pages": "npm run build-frontend && npm run prepare-pages"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
